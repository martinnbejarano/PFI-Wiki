[pytest]
# La batería se corre desde `prototipo/servicio`, que es donde vive el paquete
# `app`. `pythonpath` evita tener que instalar el paquete para poder importarlo.
pythonpath = .
testpaths = tests
